version: 0.2

phases:
  install:
    commands:
      - echo Installing...
  build:
    commands:
      - echo Building Lambda package...
      - zip function.zip lambda_function.py
artifacts:
  files:
    - function.zip
